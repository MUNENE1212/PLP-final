# Render Environment Variables Template
# Copy these to Render Dashboard > Environment Variables

# ============================================
# BACKEND SERVICE ENVIRONMENT VARIABLES
# ============================================

# Application
NODE_ENV=production
PORT=5000

# MongoDB (Use MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ementech?retryWrites=true&w=majority

# JWT (Auto-generated by Render or set manually)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_EXPIRE=30d

# Redis (Auto-configured by Render Redis service)
REDIS_HOST=<auto-filled-by-render>
REDIS_PORT=<auto-filled-by-render>

# CORS (Auto-configured to frontend URL)
CORS_ORIGIN=https://ementech-frontend.onrender.com

# M-Pesa Configuration
MPESA_CONSUMER_KEY=your-mpesa-consumer-key
MPESA_CONSUMER_SECRET=your-mpesa-consumer-secret
MPESA_PASSKEY=your-mpesa-passkey
MPESA_SHORTCODE=174379
MPESA_ENVIRONMENT=sandbox
MPESA_CALLBACK_URL=https://ementech-backend.onrender.com/api/v1/payments/mpesa/callback

# Optional: Email Configuration
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=noreply@ementech.com
# FROM_NAME=EmEnTech

# Optional: AWS S3 for file uploads
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=ementech-uploads

# ============================================
# FRONTEND SERVICE ENVIRONMENT VARIABLES
# ============================================

# API URL (points to backend service)
VITE_API_URL=https://ementech-backend.onrender.com/api/v1

# ============================================
# NOTES
# ============================================
#
# 1. MongoDB Atlas Setup:
#    - Create free cluster at https://www.mongodb.com/cloud/atlas
#    - Whitelist Render IPs: 0.0.0.0/0 (or specific IPs)
#    - Create database user and get connection string
#
# 2. Render Redis:
#    - REDIS_HOST and REDIS_PORT are auto-configured
#    - Don't set them manually
#
# 3. M-Pesa:
#    - Get credentials from https://developer.safaricom.co.ke
#    - Use sandbox for testing
#    - Update MPESA_CALLBACK_URL with your actual backend URL
#
# 4. JWT_SECRET:
#    - Render can auto-generate this
#    - Or use: openssl rand -base64 32
#
# 5. CORS_ORIGIN:
#    - Update with your actual frontend URL
#    - Or use environment variable reference in render.yaml
